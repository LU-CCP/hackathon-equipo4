import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  containerText: {
    width: 310,
    height: 290,
    marginTop: 80
  },
  container: {
    fontSize: 30,
    textAlign: 'center',
    fontWeight: 'bold'
  }
});

export default styles;
