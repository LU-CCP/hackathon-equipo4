import React from 'react';
import { View, Image } from 'react-native';

import img from '../../resources/images/Cream Square Simple Photo Calendar.png';

const MenuP = () => {
  const bla = 'a';

  return (
    <View>
      <Image source={img} />
    </View>
  );
};

export default MenuP;
